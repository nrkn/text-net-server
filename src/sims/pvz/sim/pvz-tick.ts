import { FIXED_TICK } from '../pvz-const.js'
import { PvzState } from '../pvz-types.js'
import { levelSunSpawned } from './pvz-query.js'

// tick!

export const tick = (state: PvzState, dt: number) => { 
  // level - sun spawn, waves

  // plants

  // projectiles

  // mowers

  // zombies

  // cleanup
}